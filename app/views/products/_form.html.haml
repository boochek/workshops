= simple_form_for [category, product] ,html: { class: 'form-horizontal' } do |f|
  = f.input :title
  = f.input :description
  = f.input :price
  %br
  = f.button :submit, class: 'btn btn-primary', value: "Create product"
